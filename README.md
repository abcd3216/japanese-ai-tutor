# 🎌 日文學習 AI 助教

嗨，我是一個剛畢業、同時在增進程式能力的學生
這是我利用空檔時間做的一個vibe coding小專案。

因為我自己也在學日文，常常一句話卡住、單字背了又忘，
所以就想「乾脆自己做一個 AI 老師陪我練」，順便把最近學的 Python、
資料庫、AI 這些東西實際做出一個能用的東西。做著做著就變成現在這個小網站了。

裡面有一位 AI 老師「**小葵老師**」可以陪你聊天問問題、有背單字的翻面卡片，
還有一個我自己覺得很好玩的「**單字小恐龍**」小遊戲。你的學習紀錄可以存到雲端，
之後還能每天寄一封學習日誌到你的 Gmail。

歡迎試玩，也歡迎給我建議，我還在邊做邊學 

---

## 這個網站可以做什麼？

- **跟 AI 老師聊天**
  有問題就問小葵老師，像「て形怎麼用」「電燈的日文怎麼說」，她會用中文詳細解釋、給例句。
  文法問題她會去查我建的文法知識庫，單字、翻譯這類就用她自己的日語知識回答。

- **背單字卡**
  選好程度（N5 或 N4）後，先挑詞性（動詞／名詞／形容詞），
  就會看到一張張單字卡。正面是日文，點一下翻過來看中文意思。

- **玩單字小恐龍**
  螢幕上會出現一個中文題目，小恐龍前面跑來一個寫著日文的障礙物，
  你要判斷這個日文是不是題目的意思：對就按 `1`、錯就按 `2`。限時 60 秒，玩完成績可以存起來。

---

## 兩種使用方式

1. **只想玩玩看 → 有網址最快**
   如果我已經把它架在雲端，你只要打開網址就能直接用，**什麼都不用裝**。

2. **想自己架一份、或研究我怎麼寫的 → 照下面步驟**
   在自己電腦上跑，或部署成你自己的雲端網址（部署方式寫在 [`DEPLOY.md`](DEPLOY.md)）。

---

## 想自己在電腦上跑跑看？

> 需要先有 Python（3.10 以上）。
> 盡量把每一步在做什麼都寫清楚了，照著做應該就能跑起來。

### 第 1 步：安裝需要的套件

打開終端機，切到專案資料夾，執行：

```bash
pip install -r requirements.txt
```

這會一次把我用到的工具都安裝好。

另外要裝一個微軟的資料庫驅動程式 **「ODBC Driver 18 for SQL Server」**
（[點這裡下載](https://learn.microsoft.com/sql/connect/odbc/download-odbc-driver-for-sql-server)），
不然連不上資料庫。

### 第 2 步：填入你自己的金鑰和資料庫帳密

專案裡有一個範本檔 `.env.example`，把它複製成 `.env`：

```bash
cp .env.example .env      # 如果是 Windows，改用 copy .env.example .env
```

然後打開 `.env`，把裡面的值換成你自己的：

```env
GEMINI_API_KEY=你的_Gemini_金鑰
SQL_SERVER=你的資料庫伺服器位址
SQL_DATABASE=你的資料庫名稱
SQL_USERNAME=你的資料庫帳號
SQL_PASSWORD=你的資料庫密碼
```

- **Gemini 金鑰**：到 [Google AI Studio](https://aistudio.google.com/apikey) 申請一把（我自己是用付費的）。
- **資料庫**：需要一個你自己的 Azure SQL Database（或其他 SQL Server）。

### 第 3 步：建立資料庫的表格和單字

用你的資料庫工具，依序執行這兩個檔案：

1. `database/setup.sql` — 建立要用的表格
2. `database/seed_vocabulary.sql` — 匯入 160 個單字（N5、N4 各 80 個）

### 第 4 步：讓 AI 老師讀懂文法知識庫

執行：

```bash
python rag/embedder.py
```

這一步會把我寫的文法資料整理成 AI 查得到的格式。
第一次執行會下載一個語言模型（約 470 MB），要等一下，之後就不用再下載了。

### 第 5 步：啟動網站

```bash
streamlit run app.py
```

執行後打開瀏覽器的 `http://localhost:8501`，
在左邊輸入名字、選程度，按「開始學習」就可以用了！

---

## 你可能會遇到的狀況（我自己踩過的坑）

- **一開始連很慢、卡住？**
  我用的是 Azure 的免費版資料庫，它閒置一陣子會自動「睡著」，
  下次連線要花 1～2 分鐘把它叫醒。這時畫面會顯示「⏳ 喚醒資料庫中」，
  等一下就好，**這是正常的，不是壞掉**。

- **AI 回覆有點慢？**
  因為要連到 Google 的伺服器生成回覆，網路來回需要一點時間。
  送出後會先出現「思考中…」，然後助教會像打字一樣一段一段回你。

---

## 我用到的技術

這個專案讓我實際練到了不少東西：

| 做什麼 | 我用了什麼 |
|---|---|
| AI 對話 | Google Gemini（`gemini-2.5-flash`） |
| 文法知識庫搜尋（RAG） | ChromaDB（把文法變成 AI 查得到的向量） |
| 資料庫 | Azure SQL Server  |
| 網站畫面 | Streamlit |
| 單字複習排程 | SM-2 間隔重複演算法 |
| 每日自動寄信 | n8n |

---

## 每天寄學習日誌到 Gmail

`n8n/` 資料夾裡是我另外做的自動化：每天早上把前一天玩小恐龍的成績整理成一封信，
還會把答錯的單字依詞性分類，寄到你的 Gmail。
有興趣的話看 [`n8n/README.md`](n8n/README.md)。

